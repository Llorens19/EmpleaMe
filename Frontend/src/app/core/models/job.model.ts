export interface Job {
    slug: string;
    name: string;
    description: string;
    salary: number;
    images: [],
    img: string,
    id_cat: string;
    // favorited: boolean;
    // favoritesCount: number;
    // author: Profile;
}
